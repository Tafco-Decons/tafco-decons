import React from "react";

const NavBar = () => {
  return (
    <div
      style={{
        backgroundColor: "steelblue",
        color: "skyblue",
        padding: "3em",
        width: "100%",
      }}
    >
      Header
    </div>
  );
};

export default NavBar;
